# Hetzner Cloud API token (required)
# Get yours at: https://console.hetzner.cloud/ > Security > API Tokens
HETZNER_API_TOKEN='your-api-token-here'

# Name of your SSH key in Hetzner (optional but recommended)
# Must match the name you gave it in: Hetzner Console > Security > SSH Keys
# SSH_KEY_NAME='my-laptop'

# Server type (optional, default: cpx11)
# SERVER_TYPE='cpx11'

# Datacenter location (optional, default: ash)
# Options: ash (US), hil (US), nbg1 (DE), fsn1 (DE), hel1 (FI)
# LOCATION='ash'

# OS image (optional, default: ubuntu-22.04)
# IMAGE='ubuntu-22.04'
